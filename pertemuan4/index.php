<!-- Function -->
    <!-- Singkat nya adalah potongan program /baris-baris code yang kita buat untuk memperumdah kita untuk memprogram, code program tadi bisa kita kasih nama dan kita bisa panggil berulang ulang  -->
    <!-- 
        Ada 2 jenis function:
        - Built-in Function = Fungsi yang dimiliki oleh php
        - User-Defined Function = Fungsi yang kita buat sendiri
    -->






    <!-- Built-int Function -->
    <!-- Sudah di definisikan dari php langsung -->

        <!-- 
            Date/Time :
                - .time() = untuk menampilkan tanggal dengan format tertentu
                - .date() = untuk menampilkan jam
                - .mktime() = untuk membuat detik sendiri 
                - .strtotime() = sama dengan mktime tapi dengan format tanggal ke detik
        -->
        <!-- 
            String :
                - strlen() = untuk menghitung panjang dari sebuah string
                - strcmp() = untuk membandingkan dua buah string
                - explode() = untuk memecah sebuah string menjadi array 
                - htmlspecialchars() = untuk menjaga kita dari orang iseng yang mau masuk ke website kita (hacker)
        -->
        <!-- 
            Utility :
                - var_dump() = untuk mencetak isi dari sebuah variable, array, object
                - isset() = untuk mengecek apakah sebuah variable sudah pernah di bikin atau blum, maka yang di hasilkan adalah nilai boolean, jika sudah maka hasil true, jika blum maka false
                - empty() = untuk mengecek variable yang ada itu kosong atau tidak
                - die() = untuk memberhentikan program kita, maka program di bawah die ini tidak akan di eksekusi
                - sleep() = untuk memberhentikan sementara, misalnya sleep slama dua detik, maka setelah dua detik akan di jalankan lagi
        -->

    <!-- User-defined Function -->
    <!-- Harus di definisikan sendiri karena kita buat function sendiri -->

        <!-- 

         -->